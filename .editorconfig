# https://EditorConfig.org

# top-most EditorConfig file
root = true

[*]
charset = utf-8
end_of_line = lf
indent_size = 2
indent_style = space
insert_final_newline = true
trim_trailing_whitespace = true

# Markdown 行尾两个空格表示硬换行，编辑器不能替用户删掉
[*.md]
trim_trailing_whitespace = false