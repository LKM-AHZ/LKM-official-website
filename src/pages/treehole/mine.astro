---
import Layout from '~/layouts/BaseLayout.astro';
import MinePage from '~/features/anonymous-letter/pages/MinePage.vue';
import '~/features/anonymous-letter/styles/global.css';
import { allMenuItems } from '~/features/shell/menus';
import { filterNavbarByNames } from '~/lib/navigation';
import { t } from '~/lib/i18n';
import type { MetaData } from '~/types/core';
const metadata: MetaData = {
  title: t('page.treehole.mineTitle'),
  description: t('page.treehole.mineDescription'),
};
---

<Layout metadata={metadata} navbarLinks={filterNavbarByNames(allMenuItems, ['nav.community', 'nav.blog', 'nav.resources', 'nav.mine'])}
  ><div style="min-height: 400px"><MinePage client:load /></div></Layout
>

<script is:inline>
  (function () {
    const theme = localStorage.theme;
    const isDark = theme === 'dark' || (!theme && window.matchMedia('(prefers-color-scheme: dark)').matches);
    document.documentElement.classList.toggle('dark', isDark);
    const hue = localStorage.getItem('hue');
    if (hue) document.documentElement.style.setProperty('--hue', hue);
  })();
</script>
