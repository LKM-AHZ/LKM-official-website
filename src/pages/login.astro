---
import PageLayout from '~/layouts/PageLayout.astro';
import LoginPage from '~/components/auth/login/LoginPage.vue';

const metadata = { title: '登录' };
---

<PageLayout metadata={metadata}>
  <LoginPage client:only="vue" />
</PageLayout>
