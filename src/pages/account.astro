---
import PageLayout from '~/layouts/PageLayout.astro';
import SettingsPage from '~/components/auth/settings/SettingsPage.vue';

const metadata = { title: '账户设置' };
---

<PageLayout metadata={metadata}>
  <SettingsPage client:only="vue" />
</PageLayout>
